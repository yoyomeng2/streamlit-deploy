definition_version: '2'
entities:
  streamlit_app:
    type: streamlit
    identifier:
      name: streamlit_app
    main_file: streamlit_app.py
    pages_dir: pages
    query_warehouse: mywarehouse
    stage: stage
    artifacts:
      - streamlit_app.py
      - environment.yml
      - pages
      - common/hello.py

